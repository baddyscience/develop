#include<iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(false);

    int n, m, k;
    int s[m][2];
    cin >> n >> m >> k;
    for (int i = 0; i < m; i++) {
        cin >> s[i][0] >> s[i][1];
    }

    return 0;
}
