#include <bits/stdc++.h>
using namespace std;

int main() {
    int t, n;
    cin >> t;
    long long ti[15], di[15], li[15];
    while (t--) {
        cin >> n;
        for (int i = 0; i < n; i++) {
            cin >> ti[i] >> di[i] >> li[i];
        }
    }

    return 0;
}